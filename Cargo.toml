[package]
name = "blog_os"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.dev]
# Disable unwinding
panic = "abort"

[profile.release]
# Disable unwinding
panic = "abort"

[dependencies]
